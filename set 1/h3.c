#include <stdio.h>

int main ()
{

	int a;
	printf("enter the value :");
	scanf("%d",&a);

	if(a>100)
	{
		printf("(%d) is greater then 100 ",a);
	}
	else
	{
		printf("(%d) is not greater then 100 ",a);
    }


	}
